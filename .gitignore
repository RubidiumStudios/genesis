t/tmp
t/vaults
t/repos/vault-test/*.yml
t/repos/*/tmp.yml
t/repos/summary-test/.genesis/cached/*/last
t/repos/compiled-kit-test/new-env.yml
*.tar.gz
*.tgz

/genesis-*
/artifacts
# The POD browser is a build product, never committed.  ci/scripts/release
# generates it per release and attaches it as genesis-api-v<version>.html,
# so a copy in the tree would only ever be one someone forgot to refresh.
/docs/api.html
/docs/api-*.html
.*.sw?
.sw?
/cover_db
.vscode/
*.code-workspace
.DS_Store

.claude
.serena
GLOB*
